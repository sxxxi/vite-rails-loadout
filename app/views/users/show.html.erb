<h1 class="text-red-500">Hello, <%= @user.first_name %></h1>

<%= link_to "Delete", users_destroy_path(@user.id), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
<% if session[:user_id] == @user.id %>
  <%= link_to "Logout", sessions_destroy_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
<% end %>
